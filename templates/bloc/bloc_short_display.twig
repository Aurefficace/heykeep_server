
    <div class="col-lg-5 card card-dash p-3 mx-2 mb-3 z-depth-0">
    <div class="row">
    
    <div class="col-lg-8 pl-0">
    <img src="{{ asset('user/profile/'~ app.user.id~ '/' ~ app.user.avatar) }}" class="rounded-circle z-depth-0" alt="avatar image" >
        <h4 class="font-weight-bold red_text pt-2">{{ bloc.title }}</h4>
        <p class="small text-muted ml-1 mb-0 mx-auto"> Crée le : {{ bloc.createdDate ? bloc.createdDate|date('d/m/Y') : '' }}</p>
       
    </div>
        <div class="row mx-0">
        <div class="col-lg-10 card-dash p-3 my-2 mx-auto bg-beige">
            <p class="text-small mb-0 blue_text mx-auto">{{ bloc.description[:50] ~ ' ...' }}</p>
        </div>

    </div>
    </div>
    </div>