{#{% extends 'base.html.twig' %}#}

{#{% block title %}New Space{% endblock %}#}

{#{% block body %}#}

    <p class="h4 mb-4">Création espace</p>
    <p>Toi aussi crée ton espace !</p>
{{ include('space/_form.html.twig', {'action' : path('space_new')}) }}
<button type="btn" class="btn rounded-pill bg-red px-3 text-white font-weight-bold">{{ button_label|default('Ajouter') }}</button>
{{ form_end(form) }}
{#    <a href="{{ path('space_index') }}">back to list</a>#}
{#{% endblock %}#}
